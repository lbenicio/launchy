import SwiftUI

#if os(macOS)
    import AppKit
#endif

// Deprecated: renamed to `LaunchyApp`.
// This file exists solely to avoid breaking third-party references; no functional code.
struct DeprecatedLaunchyAppStub { }
